let languages = ["Python", "Swift", "JavaScript", "Swift", "C++", "Python"]

let languageCount = languages.count
let uniqueLanguageCount = Set(languages).count

print("The number of values in the Array are: \(languageCount), and the number of unique values are: \(uniqueLanguageCount)")
